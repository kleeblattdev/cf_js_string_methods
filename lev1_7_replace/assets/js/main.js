const text = "Sam is good at codingschool";

const textBad = text.replace("good", "bad");
const textSusi = text.replace("Sam", "Susi");

document.write(textBad+"<br>");
document.write(textSusi+"<br>");
document.write(text.replace("codingschool", "tennis"));